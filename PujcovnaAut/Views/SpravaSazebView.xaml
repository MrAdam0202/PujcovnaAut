<Window x:Class="PujcovnaAut.Views.SpravaSazebView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:PujcovnaAut.ViewModels"
        Title="Správa sazeb a kategorií" Height="500" Width="800" WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:SpravaSazebViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TabControl Grid.Row="0">

            <TabItem Header="Kategorie aut">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding KategorieCol}" SelectedItem="{Binding VybranaKategorie}"
                              AutoGenerateColumns="False" IsReadOnly="True" BorderBrush="Black" BorderThickness="1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Název" Binding="{Binding NazevKategorie}" Width="*"/>
                            <DataGridTextColumn Header="Cena/den" Binding="{Binding DenniSazba, StringFormat=C0}" Width="Auto"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Editace Kategorie" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

                        <TextBlock Text="Název kategorie:"/>
                        <TextBox Text="{Binding VybranaKategorie.NazevKategorie, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <TextBlock Text="Denní sazba (Kč):"/>
                        <TextBox Text="{Binding VybranaKategorie.DenniSazba, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                            <Button Content="+ Přidat" Command="{Binding PridatKategoriiCommand}" Width="80" Margin="0,0,5,0"/>
                            <Button Content="- Smazat" Command="{Binding SmazatKategoriiCommand}" Width="80"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </TabItem>

            <TabItem Header="Typy pojištění">
                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="300"/>
                    </Grid.ColumnDefinitions>

                    <DataGrid Grid.Column="0" ItemsSource="{Binding PojisteniCol}" SelectedItem="{Binding VybranePojisteni}"
                              AutoGenerateColumns="False" IsReadOnly="True" BorderBrush="Black" BorderThickness="1">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Název plánu" Binding="{Binding NazevPlanu}" Width="*"/>
                            <DataGridTextColumn Header="Cena/den" Binding="{Binding CenaZaDen, StringFormat=C0}" Width="Auto"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Editace Pojištění" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

                        <TextBlock Text="Název plánu:"/>
                        <TextBox Text="{Binding VybranePojisteni.NazevPlanu, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <TextBlock Text="Cena za den (Kč):"/>
                        <TextBox Text="{Binding VybranePojisteni.CenaZaDen, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                            <Button Content="+ Přidat" Command="{Binding PridatPojisteniCommand}" Width="80" Margin="0,0,5,0"/>
                            <Button Content="- Smazat" Command="{Binding SmazatPojisteniCommand}" Width="80"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </TabItem>
        </TabControl>

        <Button Grid.Row="1" Content="ULOŽIT VŠE" Command="{Binding UlozitCommand}" 
                Height="40" Background="#CCFFCC" FontWeight="Bold" Margin="0,10,0,0"/>
    </Grid>
</Window>